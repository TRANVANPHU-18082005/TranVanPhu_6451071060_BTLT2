import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF1E515D); // The dark teal from the app bar
  static const Color buttonColor = Color(0xFFF39C12); // The orange from the button
  static const Color backgroundColor = Color(0xFFF5F7FA);
}
